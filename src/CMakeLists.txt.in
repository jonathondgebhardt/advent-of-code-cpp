# File auto-generated by StartNewDay
cmake_minimum_required(VERSION 3.0...3.5)
project(day@DAY@ CXX)

add_executable(${PROJECT_NAME} @DAY@.cpp)
target_link_libraries(${PROJECT_NAME} GTest::gtest_main AOC)
include(GoogleTest)
gtest_discover_tests(${PROJECT_NAME})

if(WIN32)
    set_target_properties(${PROJECT_NAME} PROPERTIES FOLDER "Solutions")
    target_sources(${PROJECT_NAME} PRIVATE ${CMAKE_SOURCE_DIR}/inputs/@DAY@.txt ${CMAKE_SOURCE_DIR}/inputs/@DAY@_sample.txt)
endif()
